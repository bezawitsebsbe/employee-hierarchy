using Microsoft.AspNetCore.Identity;

namespace OrgHierarchyApi.Models;

public class AppUser : IdentityUser
{
    // Add custom fields if needed (e.g., public string FullName { get; set; } = string.Empty;)
}